---
title: Conversation Panel
description: Panel component with tabs for conversation and metrics
component: ConversationPanel
---

The ConversationPanel component provides a tabbed interface combining conversation display and metrics. It includes a conversation tab for message history and a metrics tab for performance data.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel />
  </div>
`}
</LiveComponent>

---

## Component Variants

### ConversationPanel

The ConversationPanel component provides tabs for conversation and metrics.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel />
  </div>
`}
</LiveComponent>

<TypeTable
  className="text-sm"
  type={{
    noConversation: {
      description: "Disable the conversation tab",
      type: "boolean",
      required: false,
      default: "false",
    },
    noMetrics: {
      description: "Disable the metrics tab",
      type: "boolean",
      required: false,
      default: "false",
    },
    conversationElementProps: {
      description: "Props to pass to the Conversation component",
      type: "Partial<ConversationProps>",
      required: false,
      default: "undefined",
    },
    textMode: {
      description: "Text mode for displaying assistant messages ('llm' or 'tts')",
      type: '"llm" | "tts"',
      required: false,
      default: '"llm"',
    },
    noTextModeToggle: {
      description: "Disable the text mode toggle button",
      type: "boolean",
      required: false,
      default: "false",
    },
    noTextInput: {
      description: "Disable the text input field in the conversation",
      type: "boolean",
      required: false,
      default: "false",
    },
  }}
/>

---

## Usage

#### Basic Usage

Display both conversation and metrics tabs.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel />
  </div>
`}
</LiveComponent>

#### Conversation Only

Show only the conversation tab.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel noMetrics={true} />
  </div>
`}
</LiveComponent>

#### Metrics Only

Show only the metrics tab.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel noConversation={true} />
  </div>
`}
</LiveComponent>

#### Text Mode Toggle

The panel includes a toggle button to switch between LLM and TTS text modes.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel noTextModeToggle={false} />
  </div>
`}
</LiveComponent>

#### Custom Conversation Props

Pass custom props to the Conversation component.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel
      conversationElementProps={{
        assistantLabel: "AI Assistant",
        clientLabel: "You",
        noAutoscroll: false
      }}
    />
  </div>
`}
</LiveComponent>

#### Without Text Input

Hide the text input in the conversation.

<LiveComponent language="tsx" withPipecat>
{`
  import { ConversationPanel } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 w-full">
    <ConversationPanel noTextInput={true} />
  </div>
`}
</LiveComponent>

---

## Features

- **Tabbed Interface**: Provides tabs for conversation and metrics
- **Text Mode Toggle**: Button to switch between LLM and TTS text display modes
- **Flexible Configuration**: Can show/hide individual tabs
- **Conversation Integration**: Uses the Conversation component for message display
- **Metrics Integration**: Uses the Metrics component for performance data
- **Customizable**: Supports all Conversation component props

## Tabs

The panel includes two tabs:
- **Conversation**: Displays message history using the Conversation component
- **Metrics**: Displays performance metrics using the Metrics component

The default tab is "conversation" unless `noConversation` is true, in which case it defaults to "metrics".

## Text Mode Toggle

When `noTextModeToggle` is false, a button appears in the panel header that allows users to toggle between:
- **LLM**: Shows text as it's generated by the LLM
- **TTS**: Shows text as it's spoken by the TTS

## Integration

The ConversationPanel component uses:
- `Tabs` component for tabbed interface
- `Conversation` component for message display
- `Metrics` component for performance data
- `Panel` component for consistent styling

It must be used within:
- `PipecatClientProvider` context (for conversation and metrics)
- `ConversationProvider` context (for conversation messages)


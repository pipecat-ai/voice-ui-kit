---
title: Conversation
description: Real-time conversation display component with message history and text input
component: Conversation
---

The Conversation component displays real-time conversation history between users and AI assistants. It automatically integrates with the Pipecat Client SDK to show messages, connection states, and provides smooth scrolling behavior.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation />
  </div>
`}
</LiveComponent>

---

## Component Variants

### Conversation

The `Conversation` component automatically integrates with the Pipecat Client SDK. It must be used within a `PipecatClientProvider` and `ConversationProvider` context.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation />
  </div>
`}
</LiveComponent>

<TypeTable
  className="text-sm"
  type={{
    classNames: {
      description: "Custom CSS classes for different parts of the component",
      type: "{ container?: string; message?: string; messageContent?: string; role?: string; time?: string; thinking?: string; }",
      required: false,
      default: "{}",
    },
    noAutoscroll: {
      description: "Disable automatic scrolling to bottom when new messages arrive",
      type: "boolean",
      required: false,
      default: "false",
    },
    assistantLabel: {
      description: "Custom label for assistant messages",
      type: "string",
      required: false,
      default: '"assistant"',
    },
    clientLabel: {
      description: "Custom label for user/client messages",
      type: "string",
      required: false,
      default: '"user"',
    },
    systemLabel: {
      description: "Custom label for system messages",
      type: "string",
      required: false,
      default: '"system"',
    },
    textMode: {
      description: "Text mode for displaying assistant messages. 'llm' shows text as generated by LLM, 'tts' shows text as spoken by TTS",
      type: '"llm" | "tts"',
      required: false,
      default: '"llm"',
    },
    noTextInput: {
      description: "Disable the text input field at the bottom of the conversation",
      type: "boolean",
      required: false,
      default: "false",
    },
  }}
/>

---

## Usage

#### Basic Usage

Display a conversation with automatic message handling and scrolling.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation />
  </div>
`}
</LiveComponent>

#### Custom Labels

Customize the labels for different message roles.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation
      assistantLabel="AI Assistant"
      clientLabel="You"
      systemLabel="System"
    />
  </div>
`}
</LiveComponent>

#### Without Text Input

Hide the text input field if you want to handle input separately.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation noTextInput={true} />
  </div>
`}
</LiveComponent>

#### Disable Autoscroll

Disable automatic scrolling to allow users to read previous messages without interruption.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation noAutoscroll={true} />
  </div>
`}
</LiveComponent>

#### TTS Text Mode

Display text as it's spoken by the TTS instead of as generated by the LLM.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation textMode="tts" />
  </div>
`}
</LiveComponent>

#### Custom Styling

Apply custom classes to different parts of the conversation.

<LiveComponent language="tsx" withPipecat>
{`
  import { Conversation } from "@pipecat-ai/voice-ui-kit";

  <div className="h-96 border rounded-lg">
    <Conversation
      classNames={{
        container: "bg-muted/50",
        message: "p-2 rounded",
        messageContent: "text-sm",
        role: "font-bold",
        time: "text-xs opacity-50",
      }}
    />
  </div>
`}
</LiveComponent>

---

## Features

- **Real-time Messages**: Automatically displays messages from the conversation store
- **Smart Scrolling**: Only scrolls to bottom if user is already at bottom
- **Connection States**: Shows appropriate UI for different connection states
- **Text Input**: Includes built-in text input for sending messages
- **Message Roles**: Displays user, assistant, and system messages with labels
- **Timestamps**: Shows timestamps for each message
- **Thinking Indicator**: Shows thinking indicator when assistant is processing
- **Flexible Text Modes**: Supports LLM or TTS text display modes

## Connection States

The component displays different UI based on connection state:

- **No messages, connecting**: Shows "Connecting to agent..." message
- **No messages, not connected**: Shows "Not connected to agent" message with description
- **No messages, connected**: Shows "Waiting for messages..." message
- **Has messages**: Displays message list with scrolling

## Scrolling Behavior

The component implements smart scrolling:
- Automatically scrolls to bottom when new messages arrive
- Only scrolls if the user is already at the bottom (doesn't interrupt reading)
- Tracks scroll position to determine if user is at bottom
- Respects `noAutoscroll` prop to disable automatic scrolling entirely

## Integration

The `Conversation` component uses:
- `usePipecatConversation` hook to get messages from the conversation store
- `usePipecatClientTransportState` to determine connection state
- `MessageContainer` and `MessageContent` components to render individual messages
- `TextInput` component for user input

It must be used within:
- `PipecatClientProvider` context (for transport state)
- `ConversationProvider` context (for message store)

